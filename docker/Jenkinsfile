pipeline {
    agent {
        label 'jenkins/slave'
    }    
    stages {
        stage('Build') {
            steps {
                sh 'java --version' 
                echo "Get working directory"
                sh 'pwd'
            }
        }
    }
}
